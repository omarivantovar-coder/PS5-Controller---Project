# Binarios compilados (se publican via GitHub Releases, no se versionan)
*.exe

# Carpetas de build temporales
build/
dist/
ahk/
ahk.zip

# Macros grabadas por el usuario (data local, no codigo fuente)
ps5_macros.ini

# Log de diagnostico local (fallos de activacion de ventana)
activar_ventana_debug.log
